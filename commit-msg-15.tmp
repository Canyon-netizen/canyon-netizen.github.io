fix: 404 页在子路径下完全失效（相对路径）+ 404 接搜索

第 15 轮。读者访问 /blog/xxx 失败时，让他少一次「重新想关键词」的摩擦 ——
从 URL 提取关键词，显示搜索入口，点击把关键词带进搜索弹层并直接出结果；
按钮同时是带 ?q= 的 <a>，禁用 JS 也能落到带关键词的搜索页。

过程中挖出一个严重缺陷：
404.html 用的是相对路径（assets/css/style.css、assets/js/page-renderer.js…）。
GitHub Pages 会把 404.html 的内容返回给任意未命中路径，于是访问 /blog/xxx 时
浏览器去请求 /blog/assets/... —— 样式与脚本全部 404，页面变成无样式的死骨架。
本站有 /blog/、/en/、/en/blog/ 三层目录，这条路径相当常见。

- 404.html 的资源引用 / basePath / 语言链接改为站点根绝对路径
- check-site.mjs 的引用校验区分相对路径（按页目录）与根绝对路径（按仓库根）

顺带修掉两个真实缺陷：
- 搜索偶发无结果：open() 只在索引未加载时依赖 loadIndex() 内部那次渲染，
  索引已就绪（缓存命中）时用户随后输入就不会渲染；现在 open() 自己渲染一次
- 404 搜索按钮原本 dispatchEvent('search:open')，但弹层与监听都更晚就绪，
  事件被丢掉；改为直接调 window.__SITE_SEARCH__.open()

新增 tools/404-search-probe.mjs 覆盖 4 种路径形态（含交互）。
验证：静态 0/0；编码 0 损坏；断言 26/26 页 206/206 条；a11y 25 页 0 问题；
响应式 176 组合 0 问题；perf CLS 全页 0；print 0 问题；search 5/5；CV PDF 1 页。
