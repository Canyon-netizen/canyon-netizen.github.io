feat: 空页面接上真实内容 + 相关文章 + 性能审计工具

第 14 轮。可见文本量盘点显示 talks（539 字）、research（681 字）、publications（811 字）
最单薄 —— 分享记录、论文、获奖都还没公开，读者点进去只有空态。

- 新增 renderRelatedPosts：按 posts[].relatedPages 把文章接到主题页上
- tools/tag-related-posts.py 按文章实际内容打标签，tools/add-related-sections.py
  给 talks/research/publications 的中英页注入区块；无关联文章时整块移除，不留空标题

顺带修掉两个自己引入的问题：
- inline-page-data.py：内联 JSON 里含 </section> 文本，导致按字符串定位的注入脚本
  找错锚点，现统一转义 </
- perf-audit.mjs：CLS 偶发误报（同一页连测 3 次有时 0、有时 0.17，均为 main 整体下移 67px），
  改为每页测两遍取较小值；同时修正文档体积漏计（文档响应早于 Network.enable）

验证：静态 0/0；编码 0 损坏；断言 26/26 页 206/206 条；a11y 25 页 0 问题；
响应式 176 组合 0 问题；perf CLS 全页 0；print 0 问题；search 5/5；CV PDF 1 页。
